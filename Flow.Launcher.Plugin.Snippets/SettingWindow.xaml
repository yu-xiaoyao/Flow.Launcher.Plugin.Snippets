<Window x:Class="Flow.Launcher.Plugin.Snippets.SettingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        Title="{DynamicResource snippets_plugin_manage_snippets}"
        Width="1200"
        Height="640"
        MinWidth="940"
        MinHeight="600"
        Closed="OnClosed"
        Loaded="OnLoaded"
        StateChanged="Window_StateChanged"
        ResizeMode="CanResize"
        UseLayoutRounding="True"
        WindowStartupLocation="CenterScreen"
        mc:Ignorable="d"

        Background="{DynamicResource PopuBGColor}"
        Foreground="{DynamicResource PopupTextColor}">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="32"
                      ResizeBorderThickness="{x:Static SystemParameters.WindowResizeBorderThickness}" />
    </WindowChrome.WindowChrome>
    <Window.InputBindings>
        <KeyBinding Key="Escape" Command="Close" />
    </Window.InputBindings>
    <Window.CommandBindings>
        <CommandBinding Command="Close" Executed="OnCloseExecuted" />
    </Window.CommandBindings>

    <Window.Resources>
        <Style x:Key="BorderSelectableBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="White" />
            <Style.Triggers>

                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="DarkGray" />
                </Trigger>

                <DataTrigger Binding="{Binding IsSelected}" Value="True">
                    <Setter Property="Background" Value="LightBlue" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <!--  New  -->
    <Grid>
        <Border Style="{StaticResource WindowMainPanelStyle}">
            <Grid Background="{DynamicResource Color01B}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="32" />
                    <RowDefinition />
                </Grid.RowDefinitions>
                <!--  TitleBar and Control  -->
                <Image
                    Grid.Row="0"
                    Grid.Column="0"
                    Width="16"
                    Height="16"
                    Margin="10 4 4 4"
                    RenderOptions.BitmapScalingMode="HighQuality"
                    Source="/Images/Snippets.png" />
                <TextBlock
                    Grid.Row="0"
                    Grid.Column="1"
                    Margin="4 0 0 0"
                    VerticalAlignment="Center"
                    FontSize="12"
                    Foreground="{DynamicResource Color05B}"
                    Text="{DynamicResource flowlauncher_settings}" />

                <Button
                    Grid.Row="0"
                    Grid.Column="2"
                    Click="OnMinimizeButtonClick"
                    RenderOptions.EdgeMode="Aliased"
                    Style="{DynamicResource TitleBarButtonStyle}">
                    <Path
                        Width="46"
                        Height="32"
                        Data="M 18,15 H 28"
                        Stroke="{Binding Path=Foreground, RelativeSource={RelativeSource AncestorType={x:Type Button}}}"
                        StrokeThickness="1">
                        <Path.Style>
                            <Style TargetType="Path">
                                <Style.Triggers>
                                    <DataTrigger
                                        Binding="{Binding Path=IsActive, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}"
                                        Value="False">
                                        <Setter Property="Opacity" Value="0.5" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Path.Style>
                    </Path>
                </Button>
                <Button
                    Name="MaximizeButton"
                    Grid.Row="0"
                    Grid.Column="3"
                    Click="OnMaximizeRestoreButtonClick"
                    Style="{StaticResource TitleBarButtonStyle}">
                    <Path
                        Width="46"
                        Height="32"
                        Data="M 18.5,10.5 H 27.5 V 19.5 H 18.5 Z"
                        Stroke="{Binding Path=Foreground, RelativeSource={RelativeSource AncestorType={x:Type Button}}}"
                        StrokeThickness="1">
                        <Path.Style>
                            <Style TargetType="Path">
                                <Style.Triggers>
                                    <DataTrigger
                                        Binding="{Binding Path=IsActive, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}"
                                        Value="False">
                                        <Setter Property="Opacity" Value="0.5" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Path.Style>
                    </Path>
                </Button>
                <Button
                    Name="RestoreButton"
                    Grid.Row="0"
                    Grid.Column="3"
                    Click="OnMaximizeRestoreButtonClick"
                    Style="{StaticResource TitleBarButtonStyle}">
                    <Path
                        Width="46"
                        Height="32"
                        Data="M 18.5,12.5 H 25.5 V 19.5 H 18.5 Z M 20.5,12.5 V 10.5 H 27.5 V 17.5 H 25.5"
                        Stroke="{Binding Path=Foreground, RelativeSource={RelativeSource AncestorType={x:Type Button}}}"
                        StrokeThickness="1">
                        <Path.Style>
                            <Style TargetType="Path">
                                <Style.Triggers>
                                    <DataTrigger
                                        Binding="{Binding Path=IsActive, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}"
                                        Value="False">
                                        <Setter Property="Opacity" Value="0.5" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Path.Style>
                    </Path>
                </Button>
                <Button
                    Grid.Row="0"
                    Grid.Column="4"
                    Click="OnCloseButtonClick"
                    Style="{StaticResource TitleBarCloseButtonStyle}">
                    <Path
                        Width="46"
                        Height="32"
                        Data="M 18,11 27,20 M 18,20 27,11"
                        Stroke="{Binding Path=Foreground, RelativeSource={RelativeSource AncestorType={x:Type Button}}}"
                        StrokeThickness="1">
                        <Path.Style>
                            <Style TargetType="Path">
                                <Style.Triggers>
                                    <DataTrigger
                                        Binding="{Binding Path=IsActive, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}"
                                        Value="False">
                                        <Setter Property="Opacity" Value="0.5" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Path.Style>
                    </Path>
                </Button>


                <!-- Data Container -->
                <Grid
                    x:Name="NavView"
                    Grid.Row="1"
                    Grid.Column="0"
                    Grid.ColumnSpan="5"
                    Margin="0"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    Background="{DynamicResource Color01B}">

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="250" MinWidth="160" MaxWidth="560" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" MinWidth="300" />
                    </Grid.ColumnDefinitions>

                    <!-- Left Panel: Folder List-->
                    <Border
                        Grid.Column="0"
                        Margin="0"
                        Background="{DynamicResource Color02B}"
                        CornerRadius="4">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <Border Grid.Row="0" Background="LightBlue" Height="60" CornerRadius="4">
                                <TextBlock Text="这是顶部 (Fixed/Auto)" VerticalAlignment="Center"
                                           HorizontalAlignment="Center" />
                            </Border>

                            <Border Grid.Row="1" CornerRadius="4">

                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="*" />
                                    </Grid.RowDefinitions>

                                    <StackPanel Grid.Row="0">

                                        <Border CornerRadius="4" Height="32"
                                                Margin="8 4 8 2"
                                                Style="{StaticResource BorderSelectableBorderStyle}"
                                                DataContext="{Binding FolderAllSnippets}"
                                                MouseLeftButtonUp="AllSnippetsFolder_MouseLeftButtonUp"
                                                MouseRightButtonUp="AllSnippetsFolder_MouseRightButtonUp">
                                            <TextBlock Text="所有 Snippets" Margin="4 0" VerticalAlignment="Center" />
                                        </Border>

                                        <Border CornerRadius="4" Height="32"
                                                Margin="8 2"
                                                Style="{StaticResource BorderSelectableBorderStyle}"
                                                DataContext="{Binding FolderFavorites}"
                                                MouseLeftButtonUp="FavoritesFolder_MouseLeftButtonUp"
                                                MouseRightButtonUp="FavoritesFolder_MouseRightButtonUp">
                                            <TextBlock Text="收藏 Snippets" Margin="4 0" VerticalAlignment="Center" />
                                        </Border>
                                        <Border CornerRadius="4" Height="32"
                                                Margin="8 2 8 4"
                                                Style="{StaticResource BorderSelectableBorderStyle}"
                                                DataContext="{Binding FolderRecent}"
                                                MouseLeftButtonUp="RecentFolder_MouseLeftButtonUp"
                                                MouseRightButtonUp="RecentFolder_MouseRightButtonUp">
                                            <TextBlock Text="最近更新" Margin="4 0" VerticalAlignment="Center" />
                                        </Border>
                                        <Border CornerRadius="4" Height="32"
                                                Margin="8 2 8 4"
                                                Style="{StaticResource BorderSelectableBorderStyle}"
                                                DataContext="{Binding FolderNo}"
                                                MouseLeftButtonUp="NoFolder_MouseLeftButtonUp"
                                                MouseRightButtonUp="NoFolder_MouseRightButtonUp">
                                            <TextBlock Text="最近更新" Margin="4 0" VerticalAlignment="Center" />
                                        </Border>
                                    </StackPanel>

                                    <Border Grid.Row="1" Background="WhiteSmoke"
                                            CornerRadius="4">
                                        <TextBlock Text="FOLDER" VerticalAlignment="Center" FontSize="14"
                                                   HorizontalAlignment="Center" Margin="8 4" />
                                    </Border>

                                    <Border Grid.Row="2">
                                        <ScrollViewer VerticalScrollBarVisibility="Auto">

                                            <ItemsControl ItemsSource="{Binding Folders}">
                                                <ItemsControl.ItemTemplate>
                                                    <ItemContainerTemplate>
                                                        <Border CornerRadius="4" Height="32"
                                                                Margin="8 2 8 2"
                                                                Style="{StaticResource BorderSelectableBorderStyle}"
                                                                MouseLeftButtonUp="FolderList_MouseLeftButtonUp"
                                                                MouseRightButtonUp="FolderList_MouseRightButtonUp">

                                                            <TextBlock Text="{Binding Name}" Margin="4 0"
                                                                       VerticalAlignment="Center" />
                                                        </Border>
                                                    </ItemContainerTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>

                                        </ScrollViewer>
                                    </Border>
                                </Grid>

                            </Border>

                            <Border Grid.Row="2" CornerRadius="4" Height="44" Background="Gray">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center"
                                            HorizontalAlignment="Center">
                                    <Button Content="{DynamicResource snippets_plugin_add}" />
                                </StackPanel>
                            </Border>
                        </Grid>
                    </Border>

                    <!-- Splitter -->
                    <GridSplitter
                        Grid.Column="1"
                        Width="4"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Stretch"
                        Background="{DynamicResource Color03B}"
                        ResizeBehavior="PreviousAndNext" />

                    <!-- Right Panel: Select Folder Snippets -->
                    <Border
                        Grid.Column="2"
                        Margin="0"
                        Background="{DynamicResource Color04B}"
                        CornerRadius="4">


                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="60" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>


                            <Border Grid.Row="0">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="110" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <Border Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Center"
                                            CornerRadius="4">
                                        <ComboBox Width="100" FontSize="12" HorizontalAlignment="Center">
                                            <ComboBoxItem Content="正常" FontSize="12" />
                                            <ComboBoxItem Content="语法" FontSize="12" />
                                        </ComboBox>
                                    </Border>

                                    <Border Grid.Column="1" VerticalAlignment="Center">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <Border Grid.Column="0" VerticalAlignment="Center" Margin="4 0 0 0">
                                                <Label Content="Key" FontSize="14" />
                                            </Border>
                                            <Border Grid.Column="1" Margin="4 0 0 0">
                                                <TextBox FontSize="14" />
                                            </Border>

                                            <Border Grid.Column="2" VerticalAlignment="Center" Margin="4 0 0 0">
                                                <Label Content="Value" FontSize="14" />
                                            </Border>
                                            <Border Grid.Column="3" Margin="4 0 0 0">
                                                <TextBox FontSize="14" />
                                            </Border>

                                            <Border Grid.Column="4" VerticalAlignment="Center" Margin="4 0 0 0">
                                                <Label Content="Folder" FontSize="14" />
                                            </Border>
                                            <Border Grid.Column="5" Margin="4 0 0 0">
                                                <TextBox FontSize="14" />
                                            </Border>

                                            <Border Grid.Column="6" VerticalAlignment="Center" Margin="4 0 0 0">
                                                <Button FontSize="14" Content="Search" Width="84" />
                                            </Border>
                                            <Border Grid.Column="7" Margin="4 0 0 0">
                                                <Button FontSize="14" Content="Reset" Width="84" />
                                            </Border>

                                        </Grid>
                                    </Border>
                                    <Border Grid.Column="2" CornerRadius="4" Margin="2 0"
                                            VerticalAlignment="Center" HorizontalAlignment="Center">
                                        <Button FontSize="14" Content="Add" Width="84" />
                                    </Border>

                                </Grid>
                            </Border>

                            <Border Grid.Row="1" />

                        </Grid>


                    </Border>

                </Grid>


            </Grid>
        </Border>
    </Grid>


</Window>